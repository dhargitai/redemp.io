version: '3'

services:
  backend:
    build:
      context: ./../backend
      dockerfile: ./../backend/backend.dockerfile
    restart: always
    ports:
      - "3001:3000"
    deploy:
      mode: replicated
      replicas: 1
